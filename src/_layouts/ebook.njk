<!DOCTYPE html>
<html lang="zh">
<head>
  {% include "head.njk" %}
</head>

<style>
.article-body p {
  margin-bottom: 2em;
  line-height: 2.2;
  font-size: 16px;
}
.article-body h2 {
  font-weight: 700;
  font-size: 2rem;
  line-height: 1.6;
  margin-top: 3em;
  margin-bottom: 1.2em;
}
.article-body h3, .article-body h4 {
  font-weight: 600;
  font-size: 1.8rem;
  line-height: 1.6;
  margin-top: 2.5em;
  margin-bottom: 1em;
}
.article-body img {
  display: block;
  margin: 2em auto;
  max-width: 100%;
  height: auto;
}
.article-body p + p {
  margin-top: -1em;
}
</style>

<body>
  <main>
    {% include "header.njk" %}
    {% include "starnight.njk" %}
    {% include "article-intro.njk" %}

    <!-- 单栏布局 -->
    <div class="container g-pt-100 g-pb-20">
      <article class="g-mb-100">

        <div class="article-body g-color-black" style="line-height: 2.5; font-size: 16px;">
          {{ content | safe }}
        </div>

      </article>
    </div>

    {% include "ebook-footer.njk" %}
  </main>

  {% include "scripts.njk" %}
</body>
</html>
